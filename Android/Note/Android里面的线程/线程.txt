1.在Activity调用finish的时候界面就马上结束，但是不会立即调用onDestory等方法，
所以我们可能在见到下个界面一会儿之后上个界面才会调用onDestory方法。

2.在Activity里面的子线程可能会晚于Activity线程，
换言之就是界面线程结束之后子线程还会继续运行，
并且此时Activity里面的资源依然可以正常访问，
如界面控件等，此时给控件设置内容并不会导致异常。